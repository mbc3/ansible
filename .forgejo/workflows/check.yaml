---
on:
  push:
    branches:
      - main
enable-email-notifications: true

jobs:
  lint:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v6

      - name: Minimum linter
        run: ansible-lint --profile=moderate
